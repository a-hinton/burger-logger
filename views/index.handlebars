<h1>Eat Da Burger!</h1>

<h2>Add a Custom Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eat">Eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> This burger was eaten<br>
    <input type="radio" name="eaten" value="0"> Not yet eaten
  </div>

  <button type="submit">Add Burger</button>
</form>


<h2>Burgers available to eat.</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers which have been eaten.</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>


